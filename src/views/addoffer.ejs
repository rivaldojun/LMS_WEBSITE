<!DOCTYPE html>
<head>
    <title>LMS</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;700&display=swap" rel="stylesheet">
</head>

<body>

    <%- include('navbar.ejs', { currentPage: '' }) %>
    <div id="blg" class="container mb-4">
      <h1 class="mt-5  mb-4 text-center">Ajouter un annonce</h1>
      <form method="POST" action="/enregistrer-offer" enctype="multipart/form-data">
        <div class="form-group">
            <label for="annonce-type">Type de l'annonce :</label>
            <select id="annonce-type" name="annonce-type" class="custom-select" required>
                <option selected disabled>Sélectionnez le type d'annonce</option>
                <option value="emploi">Offre d'emploi</option>
                <option value="stage">Offre de stage</option>
            </select>
        </div>
        <div class="form-group">
            <label for="contenu">Fonction</label>
            <textarea class="form-control" id="fonction" name="fonction" rows="6" required></textarea>
        </div>
        <div class="form-group">
            <label for="contenu">Date</label>
            <input type="date" class="form-control" id="date" name="date" rows="6" required>
        </div>
          <div class="form-group">
              <label for="contenu">Contenu de l'article :</label>
              <textarea class="form-control" id="contenu" name="contenu" rows="6" required></textarea>
          </div>
          <div class="form-group">
              <label for="image">Image :</label>
              <br>
              <input type="file" id="image" name="image" accept="image/*" required>
          </div>
          <br>
          <button type="submit" class="btn btn-primary">Publier</button>
      </form>
  </div>
<%- include('footer.ejs') %>
<style>
    /* Ajoutez ce code CSS dans un fichier externe ou dans la balise <style> de votre page */

/* Style général de la page */



/* Style du formulaire */
.form-group label {
    font-weight: bold; /* Texte en gras pour les étiquettes */
}

/* Style du bouton de publication */
.btn-primary {
    background-color: #007bff; /* Couleur de fond du bouton */
    border: none; /* Supprimez la bordure */
    font-weight: bold;
}

.btn-primary:hover {
    background-color: #0056b3; /* Nouvelle couleur au survol du bouton */
}



/* Personnalisation du pied de page */
.footer {
    background-color: #333; /* Couleur de fond du pied de page */
    color: #fff; /* Couleur du texte du pied de page */
    padding: 20px;
    text-align: center;
}

/* Styles pour les inputs et textarea (si nécessaire) */
.form-control {
    border: 1px solid #ccc; /* Bordure des champs */
    border-radius: 4px; /* Coins arrondis des champs */
}

/* Style pour le champ de fichier */
.form-control-file {
    border: 1px solid #ccc;
    border-radius: 4px;
    padding: 8px;
}



</style>
</body>